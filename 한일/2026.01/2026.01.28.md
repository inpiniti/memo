1. 조정기 > 개요 > 주소 > 공동지번 village 추가

- 화면 : /safety-check/governor-list
- 작업파일 : address.js
- 내용 : 조정기 공동지번 쪽 주소 계산하는 부분에 vaillage 추가

2. [2026010268] 체납 > 가스링크 중지보류예정일자 에러 보완

- 화면 : /non-payment/non-payment-activities
- api : https://gaslink-dev.seoulgas.co.kr/api/receive/nonpayment/detail
- 백엔드에 요청 : 중지보류 예정일자 추가 요청
- 추가된 컬럼 : cutHoldYmd
- 화면상에서는 stopHoldScheduledData 라는 변수를 사용
- 작업내용 : selectedData?.detail.?cutHoldYmd 을 stopHoldScheduledData 에 할당

3. 시설전계량기등록 > 계량기등록 > 유효기간 추가

- 개발환경 세팅 : 서버 & 클라이언트 로컬 셋팅
- 시설전 조회 쿼리

  ```
  SELECT *
  FROM   C31.C3AT_FACI_BEF_PDA_WORK PW
      JOIN A11.A1AT_ZIP ZIP ON PW.ZIP_NO1 = ZIP.ZIP_NO1
  WHERE  PW.REG_END_YN = 'Y'    -- 등록 종료 여부 Y
  AND  PW.CONF_YN    = 'N'    -- 확정 여부 N (진행 중인 건)
  -- AND ZIP.CENTER_CD = '사용자센터코드'  -- 필요 시 주석 해제하여 필터링
  ORDER BY PW.WORK_NUM DESC;
  ```

- 화면 : C6C310250000F.html
  - 유효기간 추가 : mtrExpiryDate (바뀔수 있음)

- 서버 : SCG9004Request_Body_items.java
  - 유효기간 추가 : mtr_expiry_date

- 맵퍼 : scgfms_befofaci.xml
  - 테이블명 : C31.C3AT_FACI_BEF_PDA_RSLT
  - 시설전 점검 계량기 등록 : SCG9004_rslt_insert : mtrExpiryDate 추가
  - ??? : SCG9004_rslt_insert_mtrNum : mtrExpiryDate 추가
  - 시설전 점검 계량기 등록 : SCG9004_rslt_update : mtrExpiryDate 추가

- 확인해보니 유효기간이 없음 재작업 필요

- 항래차장님 문의

  ```
  SELECT  MTR_VALID_YM    -- 유효년월
      , MTR_GRD         -- 등급
      , MTR.*
  FROM    C31.C3AT_MTR  MTR
  WHERE   MTR_NUM = '302654154'
  ;



  -- 기존 기준.
  if(2 <= mtrGrd && mtrGrd <= 10) {	// 10등급이하
      nFromGrd = 2;
      nToGrd = 10;

      nFromMM = 54;
      nToMM = 60;
  }
  else if(11 <= mtrGrd && mtrGrd <= 1000) {	// 11~1000등급
      nFromGrd = 11;
      nToGrd = 1000;

      nFromMM = 90;
      nToMM = 96;
  }
  else if(1000 < mtrGrd) {	// 1000등급초과
      nFromGrd = 1000;
      nToGrd = null;

      nFromMM = 114;
      nToMM = 120;
  }




  -- 시설전에서만 적용하는 기준. (소용량계량기만 적용.  2.5 , 4 , 6 등급)

  -- 나오는거
  SELECT  C31.PKS_C3_FUNC_03.FUNC_GET_MTR_VALID_YM('226211023069')  -- 기물번호
  FROM    DUAL
  ;

  -- 안나오는거. 안나오면 999912
  SELECT  C31.PKS_C3_FUNC_03.FUNC_GET_MTR_VALID_YM('261221000001')  -- 기물번호
  FROM    DUAL
  ;



  SELECT  ADD_MONTHS(SYSDATE,114)
  FROM    DUAL
  ;
  ```

4. 업무연락단톡방 > 계량기 종류가 잘못 뜨고 있다는 내용인듯.

계량기번호 : 263315500040
내용 : 일반이 뜨고 있는데, 누출점검용(듀얼)이 떠야함
화면 : /meter-replacement/eaccounting-replacement
수정 파일 : useBarocde.jsx 
수정 내용 : asis 코드 참고 해서 누락된 로직 추가

![alt text](image.png)

![alt text](image-1.png)

5. 계량기 교체 > 무전표 조회가 안된다고 함.

- 확인했을때 조회가 됨.

6. 자율안전점검 > 해당세대가 모든 항목에 대한 결과승인으로 선택했는데 저장했을 때 팝업이 이렇게 뜨더라고요

사용계약번호 : 60050-68521
